include<stdio.h>
#include<string.h>
#define max 100
int main() 
{
   char str[max],f='b';
   int i;
   printf("enter the string to be checked: ");
   scanf("%s",str);
   int length = strlen(str);
   if(str[0] == 'b' && str[length-1] == 'b')
   {
      printf("Accepted");
   }
    else
	{
      printf("Rejected");
    return 0;
    }
}
